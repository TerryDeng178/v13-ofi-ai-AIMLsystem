# 实时模式配置 - 低延迟、用于触发灰度
# 适用于：实时交易、信号触发、低延迟场景

# 水位线配置（低延迟模式）
WATERMARK_MS=500

# Z-score配置（P1.1支持Delta-Z）
CVD_Z_MODE=level

# 其他配置
HALF_LIFE_TRADES=300
WINSOR_LIMIT=8.0
FREEZE_MIN=50

# 数据采集配置
PRINT_EVERY=1000
QUEUE_SIZE=2000
HEARTBEAT_TIMEOUT=60
BACKOFF_MAX=30

# 说明：
# - WATERMARK_MS=500: 0.5秒水位线，降低延迟，适合实时场景
# - 延迟：P95约0.7-1.2秒（网络200-300ms + 水位线500ms + 处理延迟）
# - 优势：延迟低，适合实时触发
# - 劣势：可能错过部分乱序消息，需要灰度验证
# - 建议：先在灰度环境验证四绿灯指标是否保持
