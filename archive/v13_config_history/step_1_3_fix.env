# Step 1.3: 修复实现问题 + 回到Step 1.1较优组合
CVD_Z_MODE=delta
HALF_LIFE_TRADES=300
WINSOR_LIMIT=8.0
STALE_THRESHOLD_MS=5000
FREEZE_MIN=80

# 混合尺度地板（回到Step 1.1较优权重，但修复实现问题）
SCALE_MODE=hybrid
EWMA_FAST_HL=80
SCALE_FAST_WEIGHT=0.25
SCALE_SLOW_WEIGHT=0.75
MAD_WINDOW_TRADES=300
MAD_SCALE_FACTOR=1.4826
MAD_MULTIPLIER=1.40

# 空窗后软冻结（在 real_cvd_calculator.py 中实现）
# POST_STALE_FREEZE 参数不再直接控制，而是由内部逻辑根据 interarrival_ms 动态决定
