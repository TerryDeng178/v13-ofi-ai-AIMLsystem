# Step 1.6 生产基础版 - 分析模式配置
# 适用于：分析、研究、低风险策略灰度

# Delta-Z 核心参数
CVD_Z_MODE=delta
HALF_LIFE_TRADES=300
WINSOR_LIMIT=8.0
STALE_THRESHOLD_MS=5000
FREEZE_MIN=80

# 混合尺度地板
SCALE_MODE=hybrid
EWMA_FAST_HL=80
SCALE_FAST_WEIGHT=0.35
SCALE_SLOW_WEIGHT=0.65
MAD_WINDOW_TRADES=300
MAD_SCALE_FACTOR=1.4826
MAD_MULTIPLIER=1.45

# 水位线配置（分析模式）
WATERMARK_MS=2000

# 质量目标
# P(|Z|>2) <= 8% (硬线)
# P(|Z|>3) <= 2% (优化项)
# median(|Z|) <= 1.0
