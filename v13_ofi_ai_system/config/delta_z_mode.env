# Delta-Z模式配置 - P1.1稳健Z-score，用于Z质量优化
# 适用于：Z-score质量优化、稳健性测试、Delta域标准化

# 水位线配置（稳健模式）
WATERMARK_MS=2000

# Z-score配置（P1.1 Delta-Z模式）
CVD_Z_MODE=delta

# Delta-Z专用配置
HALF_LIFE_TRADES=300
WINSOR_LIMIT=8.0
FREEZE_MIN=50
STALE_THRESHOLD_MS=5000

# 数据采集配置
PRINT_EVERY=100
QUEUE_SIZE=1000
HEARTBEAT_TIMEOUT=60
BACKOFF_MAX=30

# 说明：
# - CVD_Z_MODE=delta: 启用Delta-Z模式，基于增量域标准化
# - HALF_LIFE_TRADES=300: EWMA半衰期约5分钟（中频交易）
# - WINSOR_LIMIT=8.0: Z-score截断阈值，避免极端值
# - FREEZE_MIN=50: 暖启动期50笔交易
# - STALE_THRESHOLD_MS=5000: 5秒空窗后冻结Z-score
# - 优势：Z-score分布更稳健，尾部风险降低
# - 劣势：计算复杂度略增，需要更多配置调优
