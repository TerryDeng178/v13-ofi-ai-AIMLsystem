# Step 1: 保守微调 + 稳健尺度地板配置
# 目标: P(|Z|>2) ≤ 8%, P(|Z|>3) ≤ 2%

# 核心参数
CVD_Z_MODE=delta
HALF_LIFE_TRADES=290        # 从300小幅降到290（保守）
WINSOR_LIMIT=8.0            # 维持8，不再"提前削顶"
STALE_THRESHOLD_MS=5000     # 维持5s（避免空窗后首笔误报）
FREEZE_MIN=60               # 暖启动样本≥60笔再产出Z

# 稳健尺度地板（新）
SCALE_MODE=hybrid           # {ewma|hybrid}
EWMA_FAST_HL=80             # 快EWMA半衰期≈80笔
MAD_WINDOW_TRADES=300       # MAD窗口，≈P1.1基线尺度
MAD_SCALE_FACTOR=1.4826     # 把MAD还原为σ的一致性系数

# 水位线配置（分析模式）
WATERMARK_MS=2000
