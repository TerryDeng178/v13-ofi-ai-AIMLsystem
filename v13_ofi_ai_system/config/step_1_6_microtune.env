# Step 1.6 基线配置（CVD Z-score微调优化基线）
# 基于Task_1.2.10.1修复后的生产基线配置

# CVD计算模式
CVD_Z_MODE=delta

# 核心参数
HALF_LIFE_TRADES=300
WINSOR_LIMIT=8.0
STALE_THRESHOLD_MS=5000
FREEZE_MIN=80

# 软冻结参数
SOFT_FREEZE_MS=4000
HARD_FREEZE_MS=5000

# 尺度计算
SCALE_MODE=hybrid
EWMA_FAST_HL=80
SCALE_FAST_WEIGHT=0.35
SCALE_SLOW_WEIGHT=0.65

# MAD参数
MAD_WINDOW_TRADES=300
MAD_SCALE_FACTOR=1.4826
MAD_MULTIPLIER=1.45

# 软冻结V2（新增特性开关）
SOFT_FREEZE_V2=0
SOFT_FREEZE_GAP_MS_MIN=4000
SOFT_FREEZE_GAP_MS_MAX=5000
SOFT_FREEZE_MIN_TRADES=1

# 基线指标（Step 1.6）
# P(|Z|>2): 5.73% ✅
# P(|Z|>3): 4.65% ⚠️ (目标≤2%)
# median(|Z|): 0.0013 ✅
# 数据完整性: 100% ✅
# 系统稳定性: 100% ✅
