# Step 1.6 干净金测配置
# 目标：queue_drop_rate=0%、duration≥35min、P(|Z|>2)≤8%、P(|Z|>3)≤2%

# 基础配置
CVD_Z_MODE=delta
HALF_LIFE_TRADES=300
WINSOR_LIMIT=8.0
STALE_THRESHOLD_MS=5000
FREEZE_MIN=80

# 混合尺度配置
SCALE_MODE=hybrid
EWMA_FAST_HL=80
SCALE_FAST_WEIGHT=0.35
SCALE_SLOW_WEIGHT=0.65
MAD_WINDOW_TRADES=300
MAD_SCALE_FACTOR=1.4826
MAD_MULTIPLIER=1.45

# 水位线配置（分析模式）
WATERMARK_MS=2000
POST_STALE_FREEZE=2

# 日志级别（减少阻塞）
LOG_LEVEL=INFO
