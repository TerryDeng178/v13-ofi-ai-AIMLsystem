# Step 1 微调配置 - 保守稳进方案
# 目标：P(|Z|>2) ≤ 8%, P(|Z|>3) ≤ 2%

# 基础Delta-Z配置
CVD_Z_MODE=delta
HALF_LIFE_TRADES=295
WINSOR_LIMIT=8.0
STALE_THRESHOLD_MS=5000
FREEZE_MIN=80

# 稳健尺度地板（微调）
SCALE_MODE=hybrid
EWMA_FAST_HL=80
MAD_WINDOW_TRADES=300
MAD_SCALE_FACTOR=1.4826

# Step 1 微调参数
SCALE_FAST_WEIGHT=0.30
SCALE_SLOW_WEIGHT=0.70
MAD_MULTIPLIER=1.30
POST_STALE_FREEZE=2
