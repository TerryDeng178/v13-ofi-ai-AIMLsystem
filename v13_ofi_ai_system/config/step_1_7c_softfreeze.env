# Step 1.7-C 软冻结配置（CVD Z-score微调优化）
# 基于Step 1.6基线 + 软冻结V2特性

# CVD计算模式
CVD_Z_MODE=delta

# 核心参数（保持基线）
HALF_LIFE_TRADES=300
WINSOR_LIMIT=8.0
STALE_THRESHOLD_MS=5000
FREEZE_MIN=80

# 软冻结参数（保持基线）
SOFT_FREEZE_MS=4000
HARD_FREEZE_MS=5000

# 尺度计算（保持基线）
SCALE_MODE=hybrid
EWMA_FAST_HL=80
SCALE_FAST_WEIGHT=0.35
SCALE_SLOW_WEIGHT=0.65

# MAD参数（保持基线）
MAD_WINDOW_TRADES=300
MAD_SCALE_FACTOR=1.4826
MAD_MULTIPLIER=1.45

# 软冻结V2（新增特性开关 - 启用）
SOFT_FREEZE_V2=1
SOFT_FREEZE_GAP_MS_MIN=4000
SOFT_FREEZE_GAP_MS_MAX=5000
SOFT_FREEZE_MIN_TRADES=1

# 预期效果
# 针对静默后首笔异常Z-score进行保护
# 指标：z_after_silence_p3 应显著低于基线
# 目标：P(|Z|>3) 从4.65%压降到≤2%
