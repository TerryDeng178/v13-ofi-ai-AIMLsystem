# 分析模式配置 - 稳健、用于离线/分析
# 适用于：数据采集、回测分析、研究验证

# 水位线配置（稳健模式）
WATERMARK_MS=2000

# Z-score配置（当前P0-B模式）
CVD_Z_MODE=level

# 其他配置
HALF_LIFE_TRADES=300
WINSOR_LIMIT=8.0
FREEZE_MIN=50

# 数据采集配置
PRINT_EVERY=100
QUEUE_SIZE=1000
HEARTBEAT_TIMEOUT=60
BACKOFF_MAX=30

# 说明：
# - WATERMARK_MS=2000: 2秒水位线，确保消息重排序，适合分析场景
# - 延迟：P95约4-5秒（网络200-300ms + 水位线2000ms + 处理延迟）
# - 优势：数据完整性100%，适合离线分析
# - 劣势：延迟较高，不适合实时触发
