# Step B: 稳健尺度地板 - 混合尺度保护
# 在Step A基础上，引入稳健尺度地板避免静默后分母过小

# 水位线配置（稳健模式）
WATERMARK_MS=2000

# Z-score配置（Step B稳健尺度）
CVD_Z_MODE=delta
HALF_LIFE_TRADES=280        # 保持Step A设置
WINSOR_LIMIT=8.0            # 保持基线不变，不再降低
STALE_THRESHOLD_MS=5000     # 保持基线不变
FREEZE_MIN=60               # 保持Step A设置

# 稳健尺度配置（需要在代码中实现）
# 方案1: 双EWMA加权平均
# scale = 0.5*ewma_fast(80) + 0.5*ewma_slow(280)
# 
# 方案2: 混合尺度地板
# scale = max(EWMA(|Δ|), 1.4826*MAD_300)

# 目标：P(|Z|>2)≤8%、P(|Z|>3)≤2%
# 只有当Step A达不到<3%时，才启用Step B
