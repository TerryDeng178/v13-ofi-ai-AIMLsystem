# Step A: 保守微调 - 仅调整半衰期
# 在P1.1基线基础上，仅把半衰期小幅调低到280

# 水位线配置（稳健模式）
WATERMARK_MS=2000

# Z-score配置（Step A保守微调）
CVD_Z_MODE=delta
HALF_LIFE_TRADES=280        # 从300小幅调低到280（-6.7%）
WINSOR_LIMIT=8.0            # 保持基线不变
STALE_THRESHOLD_MS=5000     # 保持基线不变
FREEZE_MIN=60               # 从50调整到60（+20%，更稳健的暖启动）

# 目标：P(|Z|>2)≤8%、P(|Z|>3)≤3%
# 若任一劣化，直接退回300
